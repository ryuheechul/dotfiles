# Automatically generated by espanso migration tool
# Original file: default.yml

matches:
  - trigger: ":espanso"
    replace: Hi there!
  - trigger: ":date"
    replace: "{{mydate}}"
    vars:
      - name: mydate
        type: date
        params:
          format: "%m/%d/%Y"
  - trigger: ":shell"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: echo Hello from your shell
  - trigger: ":upcase"
    replace: "{{output}}"
    vars:
      - name: form1
        type: form
        params:
          layout: |
            to uppercase [[to_upper]]
      - name: output
        type: shell
        params:
          cmd: echo '{{form1.to_upper}}' | upcase
  - trigger: ":downcase"
    replace: "{{output}}"
    vars:
      - name: form1
        type: form
        params:
          layout: |
            to lowercase [[to_lower]]
      - name: output
        type: shell
        params:
          cmd: echo '{{form1.to_lower}}' | downcase
