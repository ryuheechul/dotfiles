# gh-dash

Install with `gh extension install dlvhdr/gh-dash`.

And [config.yml][./config.yml] with https://www.gh-dash.dev/getting-started/usage/.

Symlink will be set via ../../bootstrap/configuration.sh.
