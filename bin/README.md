# bin
Where scripts are

```bash
bin/                # scripts for any platform
├── path/           # things that is supposed to be added to $PATH
│   ├── default/    # for every system
│   ├── wsl/        # only for WSL
│   └── darwin/     # only for Darwin
├── darwin/         # masOS specific scripts
└── wsl/            # WSL specific scripts
```
