#!/usr/bin/env bash

script_d="$(cd "$(dirname "$0")" && pwd)"

git diff "${HOME}/.doom-emacs.d/static/init.example.el" "${script_d}/../init.el"
git diff "${HOME}/.doom-emacs.d/static/config.example.el" "${script_d}/../config.el"
